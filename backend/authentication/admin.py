from django.contrib import admin

# Register your models here.
admin.site.register([User, DoctorProfile, PatientProfile, PastMetrics])admin.site.register([User, DoctorProfile, PatientProfile, PastMetrics])